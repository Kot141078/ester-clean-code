{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ester.serviceAccountName" . }}
  labels:
    {{- include "ester.labels" . | nindent 4 }}
{{- end }}
