# Continuity Bundle / Cold Wake v0.1

**Status:** Implementation-facing companion, canonical in AGI  
**Layer relation:** continuity preservation and bounded cold-wake companion for runtime surfaces  
**Language:** English

---

## Why it matters here

This repository is where continuity claims have to meet concrete runtime surfaces.
Cold wake is not only a theory problem here; it is a question of what can be preserved, mounted, constrained, and resumed without bypassing reviewable gates.

## Mapping to concrete runtime surfaces

- layered memory / preserved state: `storage/`, `modules/`
- identity / passport / anchor surfaces: `roles/`, `volition/`, `security/`
- provider decoupling and local exchange: `lan/`, `p2p/`, `messaging/`
- oracle gating / oracle window: `middleware/`, `windows/`, `scheduler/`, `routes/`
- witness / integrity surfaces: `merkle/`, `validator/`, `hashes/`
- plan-only / run-safe / fail-closed posture: `security/`, `middleware/`, `selfevo/`, `proactive/`

## Canonical home

Canonical entry:
`..\..\advanced-global-intelligence\protocols\continuity-bundle\README.md`

## Explicit bridge

The AGI package defines what must be preserved and how cold wake should be classified.
This repository matters because those requirements only become operational when identity, memory, witness, oracle gating, and fail-closed controls are attached to real runtime surfaces.

## Hidden bridge #1

Provider substitution is not continuity by itself.
Continuity depends on whether memory, constraints, and anchor-bound control survive the move into a new runtime.

## Hidden bridge #2

A bundle is not enough if resume bypasses gates.
Cold wake remains weaker than a normal run until witness, budgets, and bounded motor access are re-established.

## Earth paragraph

A backup that cannot be mounted safely is only storage.
A process that boots without its gates is only motion.
Implementation-facing continuity starts where preserved material can be reattached to bounded runtime surfaces without pretending that successful execution is the same thing as justified resume.
