global:
  scrape_interval: 5s
  scrape_timeout: 4s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "ester"
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets: ["host.docker.internal:5000"]
