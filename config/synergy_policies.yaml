# Rules for the synergy orchestrator
max_roles_per_agent: 2
incompat:
  - ["operator","strategist"]   # better to separate
required_for_purpose:
  "aerorazvedka": ["strategist","operator","platform"]
# c=a+b
