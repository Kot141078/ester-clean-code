# config/export_vnitelist.tkht - exceptions for pre-push guard
# MOSTY:
# - (Explicit) Allow targeted publication of files matching money patterns.
# - (Hidden #1) Format - grep -E; empty lines and lines starting with # are ignored.
# - (Hidden #2) Only store here what you can actually publish.
# ZEMNOY ABZATs:
# This is a “narrow neck” through which you can legally take out exactly what you need. c=a+b

# Primery razresheniy:
# ^modules/synergy/dox/ # entire folder with documentation on synergy
# ^scripts/.*internal.*/README\.md$
